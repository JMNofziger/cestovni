# Spec: Export v1 (ZIP + CSV + manifest)

**Status:** Stub  
**Linear:** CES-28

## Scope

- On-device assembly; **streaming** / disk-backed writes; snapshot semantics (SQLite read transaction + manifest fields for pending backup).
- **No** receipt photos in export (ephemeral local-only per brief).

## References

- [`PRODUCT_BRIEF.md`](../product/PRODUCT_BRIEF.md)
