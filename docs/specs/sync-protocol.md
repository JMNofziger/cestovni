# Spec: Backup protocol & v1.x sync roadmap

**Status:** Stub — **blocked** until ADR 002 accepted (may draft outline in parallel).  
**Linear:** CES-31 (blocked by CES-24)

## Scope

- v1: outbox, idempotency, retries, row versioning, fill-up lifecycle (draft/complete/amended).
- v1.x: live sync merge rules (e.g. odometer max-wins) — pointer only until scoped.

## References

- [`adr/002-backup-sync-layer.md`](adr/002-backup-sync-layer.md)
